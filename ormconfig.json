{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "password",
  "database": "nestapi",
  "logging": true,
  "entities": ["dist/**/*.entity{.ts,.js}"],
  "synchronize": false,
  "migrations": ["migration/*.{.ts,.js}"],
  "cli": {
    "migrationsDir": "migration"
  }
}
